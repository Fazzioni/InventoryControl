unit UEstoque_Aux_New;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ComCtrls;

type
  TFEstoque_AUX_New = class(TForm)
    Label1: TLabel;
    Label2: TLabel;
    ProgressBar1: TProgressBar;
    procedure FormKeyDown(Sender: TObject; var Key: Word;
      Shift: TShiftState);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FEstoque_AUX_New: TFEstoque_AUX_New;

implementation

{$R *.dfm}

procedure TFEstoque_AUX_New.FormKeyDown(Sender: TObject; var Key: Word;
  Shift: TShiftState);
begin
key:=0;
end;

end.
