unit UFinfoPedido;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ComCtrls;

type
  TFinfoPedido = class(TForm)
    ProgressBar1: TProgressBar;
    Label1: TLabel;
    Label2: TLabel;
    Label3: TLabel;
    procedure FormKeyDown(Sender: TObject; var Key: Word;
      Shift: TShiftState);
    procedure FormKeyPress(Sender: TObject; var Key: Char);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FinfoPedido: TFinfoPedido;

implementation

{$R *.dfm}

procedure TFinfoPedido.FormKeyDown(Sender: TObject; var Key: Word;
  Shift: TShiftState);
begin
key:=0;
end;

procedure TFinfoPedido.FormKeyPress(Sender: TObject; var Key: Char);
begin
key:=#0;

end;

end.
